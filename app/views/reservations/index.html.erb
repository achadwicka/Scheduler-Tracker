<center>
<div class="back">
  <%= link_to "Volver", :back %>
</div> 
<div class="card" style="width:70%; margin-top:-20px;">
  <div class="card-body">
    <h1 class="card-title">Nueva Reserva: Elige tu Doctor</h1>

      <div class="container">
      
        <p>En la siguiente tabla se muestran los doctores</p>     
        <table class="table table-bordered">
        <thead>
          <tr>
            <th> Nombre </th>
            <th> Email </th>
            <th colspan="4"></th>
          </tr>
        </thead>
          <tbody>
          <% @doctors.each do |doctor| %>
            <tr>
              <td><%= doctor.first_name %> <%= doctor.last_name %></td>
              <td><%= doctor.email %></td>
              <td><%= link_to 'Reservar', pick_date_path(:doctor => doctor), class: "btn btn-outline-primary" %></td>
            </tr>
          <% end %>
        </tbody>
        </table>
      </div>
    
</div>
</div>
</center>
